# 协同项目管理

本上下文描述游戏版本中需求与工作单的业务层级，以及项目管理工具需要追踪的核心对象。

## Language

**版本**:
承载一组核心需求和一个优化需求总单的发布周期。一个核心需求或优化需求只属于一个版本。

**活跃版本**:
仍处于自动同步、进度计算、卡片采集和通知范围内的版本。结束版本保留历史数据，但不再进入常规同步与进度采集。

**同步起点**:
某一版本开始进入自动同步范围的时间边界，由系统管理员启用该版本时确定。起点前不做版本级历史全量初始化；起点后首次命中的需求只补齐其完成判断所需的当前工作单结构。

**核心需求**:
版本内规模较大、需要多个专业共同交付的需求，其约定工单类型为“需求”，工作拆分包含核心专业子单以及测试、验收工作。
_Avoid_: 大需求

**需求量级**:
需求和核心专业子单各自持有的当前规模分类，用于开发历时等分组统计。系统按计划工作日建议小型（不超过 5 日）、中型（6–10 日）、大型（11–20 日）或特大型（超过 20 日），分界可配置且允许人工修改；修改核心需求量级时，当前全部子单随之更新，之后仍允许单个子单独立调整自身量级。历史统计始终使用记录的当前量级重新计算，不保存统计量级快照；未配置时单独归组。

**优化需求总单**:
版本内汇总全部优化需求的容器，其约定工单类型为“优化需求”。

**优化需求**:
优化需求总单下开发周期较短的改进需求，其约定工单类型为“优化需求”。单一职能可以完成时不必建立专业任务单。
_Avoid_: 小需求

**核心专业子单**:
核心需求下按专业拆分、约定工单类型为“子需求”的工作单；同一需求可以有多个同专业子单。职能包括 UI、美术、动效、数值、客户端、服务端、KGUI、礼包、模块开关和后台。缺少职能时属于配置异常，不根据标题猜测职能。
_Avoid_: 核心任务、核心工单

**基础模块**:
核心需求中达到首个可通知开发节点所需的核心专业子单集合。默认职能为 UI、美术、数值、客户端、服务端和 KGUI，范围可以按版本或需求覆盖。配置后不存在任何实际工作单时视为“基础模块未配置”，不会自动完成或发送通知；需求负责人可以明确标记“基础模块不适用”并跳过该节点。

**后续模块**:
核心需求在基础模块完成后继续制作的核心专业子单集合。默认职能为动效和后台，只统计实际存在的工作单，范围可以按版本或需求覆盖。

**全部内容完成**:
核心需求下核心专业、辅助、测试、缺陷、验收等全部后代工作单均进入终态的状态。新增或重新打开非终态后代工作单时，该状态失效。

**管理看板**:
面向项目管理行动的多层视图，默认汇总全部活跃版本的风险、异常和待协调事项，并支持下钻到版本、需求、工作单和人员。完成率与历史趋势是辅助信息，结束版本只进入历史查询。

**版本进度**:
版本内核心需求分别按是否达到基础模块完成和全部内容完成等权汇总，优化需求按是否完成等权汇总。需求包含的子单数量不构成版本进度权重，只作为拆解规模展示。

**工作日历**:
用于计算计划周期、延期和开发历时的公司工作日期集合，默认周一至周五，并由系统管理员维护节假日、调休和特殊工作日。同一天开始并完成计为一个工作日。

**开发历时**:
核心专业子单从首次进入开发状态到进入终态所经历的工作日，不代表人员实际投入工时。首次进入开发状态所对应的 Teambition 状态集合需要单独确认并支持配置。

**本轮开发历时**:
核心专业子单当前一次开发周期内累计的开发历时；完成后重新打开会开始新一轮。明确配置的暂停状态不计入本轮开发历时。

**累计开发历时**:
核心专业子单历次开发周期的开发历时总和，与本轮开发历时和重开次数一同保留。

**开发历时统计**:
对选定版本或日期范围内、真正交付完成且具备可靠状态时间的核心专业子单，按需求量级和职能分组进行的历史分布统计。每组展示样本数、中位数及最短和最长开发历时，样本达到 10 条后增加 P75、P90；平均值仅作辅助。

**业务风险**:
由阻塞、逾期、进度落后或里程碑延误反映的交付风险。业务风险与跟进风险、数据异常分别展示，不互相替代。

**风险等级**:
业务风险分为高风险、需关注和正常三级。里程碑逾期未完成、健康状态为阻塞或负责人明确表示无法按期完成属于高风险；里程碑距截止不超过 2 个工作日且仍未开始、当前周期无有效反馈，或负责人报告存在风险或可能延期属于需关注；正常推进且近期有有效反馈并确认可按期完成属于正常。单纯“尚未完成”不触发风险，阈值采用系统默认、版本覆盖和需求覆盖。

**计划进度**:
百分比跟进工作单根据计划开始日至截止日的工作日线性计算的参考进度，只用于识别需关注项，不直接产生高风险。里程碑跟进不使用线性计划进度。

**风险汇总**:
需求或版本以范围内最高的当前风险等级为标识，同时展示各等级数量和具体原因，不计算平均风险分。核心需求分别保留基础模块风险和全部内容风险。

**风险峰值**:
一次风险持续期间曾达到的最高风险等级。风险解除后峰值、原因、首次出现时间、解除时间和持续工作日保留用于历史复盘，但不影响当前风险颜色。

**跟进风险**:
负责人未在约定时间提交有效进度所形成的管理关注项，不直接表示工作交付已经高风险。

**人员并发关注**:
人员同时承担的核心专业子单数量或计划时间重叠达到可配置阈值的提示，默认阈值为 3 个，可按系统或职能调整。它只表示需要管理者复核，不等同于人员超载。

**结构完成率**:
按工作单是否进入任一终态计算的流程收口比例，已拒绝、已关闭、取消和无需处理均可计入。结构完成率不等于实际交付比例。

**实际交付数量**:
进入明确交付完成状态的工作单数量，不包含已拒绝、取消或无需处理等非交付终态；具体状态集合需要结合租户工作流确认并支持配置。

**数据覆盖率**:
某项指标中具备全部必要数据、能够参与计算的记录数占应统计记录总数的比例。覆盖率不低于 90% 为正常、70% 至 90% 为数据不完整、低于 70% 为指标暂不可靠；缺失数据不按零值计入。

**人员工作分布**:
按人员展示核心专业子单数量、职能、状态、阻塞、延期、计划工作日及已完成子单开发历时的管理视图。展示指标允许后续配置增减，但不得形成绩效分、效率分或人员排名。

**测试总单**:
核心需求下汇总测试工作、工单类型为“测试任务”的工作单，其下可以建立缺陷单。

**缺陷单**:
测试总单下记录具体缺陷、工单类型为“缺陷”的工作单。

**验收总单**:
核心需求下汇总验收工作的工作单，工单类型为“任务”，并以“工单分类=验收总单”作为稳定标记。

**验收任务单**:
验收总单下记录具体验收工作的子任务，优先根据父级是验收总单来识别。预期工单类型为“任务”；类型为空或不符时仍参与验收和全部内容完成判定，但记录配置异常。

**优化专业任务单**:
优化需求下按需要建立、工单类型为“任务”的专业工作单。可选职能包括数值、客户端、服务端、动效和 UI。
_Avoid_: 优化子单

**辅助子单**:
核心需求直属、不属于核心专业、测试或验收工作的“任务”工单，例如红点、系统引导、压测、兼容方案和音效。辅助子单默认不参与基础模块完成判定；不根据是否存在子任务或标题文字判断其角色。

**工单类型**:
Teambition 中用于表示工作单结构类别的分类。约定值为“需求”“子需求”“测试任务”“缺陷”“优化需求”和“任务”；具体领域角色还可能取决于父级工作单和工单分类，工单类型不直接决定进度范围。

**工单分类**:
任务上的结构化业务标记，用来区分使用同一工单类型的特殊领域角色。MVP 使用“验收总单”标记验收总单；它不同于工单类型和内容分类。

**职能**:
描述工作单由哪个专业职能负责的结构化分类，例如 UI、美术、动效、数值、客户端、服务端、KGUI 和后台。

**内容分类**:
对任务工作内容的业务分类，不用于区分核心需求和优化需求。
_Avoid_: 需求类型

**同步记录**:
钉钉 AI 表格中与一个 Teambition 工作单一一对应的记录，以来源系统和 Teambition 工作单 ID 唯一识别。工单移动或类型变更时更新原记录，不生成重复记录。

**初始化方式**:
同步记录最初进入钉钉 AI 表格的方式，取值为“自动同步”或“手动补录”。初始化方式不改变记录的数据来源和字段权威边界。

**纯手工记录**:
没有对应 Teambition 工作单、仅由用户在钉钉 AI 表格中建立的记录。纯手工记录不填写 Teambition 工作单 ID，也不进入自动同步；后续只能由系统管理员显式绑定 Teambition 工单。

**来源字段**:
由 Teambition 权威维护的工作单事实，包括标题、状态、父子结构、工单类型、职能、执行者、参与者和计划时间。手动补录的同步记录在关联 Teambition 工作单更新后，来源字段仍随 Teambition 更新。

**管理字段**:
仅在钉钉侧维护的项目管理信息，包括跟进方式、卡片反馈、风险记录、通知配置和管理备注。Teambition 同步不得覆盖管理字段。

**来源状态**:
同步记录所对应 Teambition 工作单的生命周期状态，分为正常、来源已归档和来源已删除。归档或删除不物理删除同步记录；来源恢复时继续使用原 Teambition 工作单 ID 对应的记录。

**同步失败**:
Teambition 变更尚未成功反映到同步记录的状态。失败变更保留为待处理对象，并通过重试或对账恢复；连续失败达到管理阈值时通知系统管理员。

**同步审计**:
对 Teambition 变更处理过程的可追溯记录，以来源事件和工作单为主线，保留变更前后值、重试次数、处理结果和处理时间。

**对账**:
周期性重新读取活跃版本的 Teambition 当前状态，并与同步记录比较和修复差异的过程。对账用于保证最终当前状态正确，不保证还原中断期间每个短暂中间状态。

**待配置工作单**:
已进入同步范围但缺少职能或必要工单分类的工作单。系统同步并展示该工作单，但不猜测缺失字段；修正前暂停受其影响的完成通知。

**人员映射**:
经系统管理员确认的 Teambition 人员 ID 与钉钉 userId 对应关系。手机号或邮箱完全一致只能生成待确认建议，不允许仅按姓名自动匹配。

**人员待匹配**:
已从 Teambition 同步但尚无已确认钉钉 userId 的人员。其工作单继续同步，但系统不得向候选人员发送消息，并通知需求负责人和系统管理员处理。

**进度采集**:
开发过程中由系统主动请执行者更新当前进度的周期性活动。默认每周二、周四 10:00 触发，只覆盖未进入终态且有执行者的核心专业子单；优化需求、测试、缺陷、验收和辅助工作单不进入常规进度采集范围。

**跟进方式**:
核心专业子单采集进度的方法，分为百分比和里程碑。默认以 10 个计划工作日为分界生成建议，分界值可按系统、版本或需求配置，需求负责人可以人工覆盖；首次反馈后不因日期变化自动切换。

**百分比跟进**:
以 0%、25%、50%、75%、100% 五档采集负责人自报进度。达到 100% 不等于工作单完成，完成仍以 Teambition 终态为准。

**里程碑跟进**:
使用 3–5 个带计划完成日期的钉钉侧管理节点采集大型核心专业子单进度；里程碑不是 Teambition 工单，也不反向写入 Teambition。节点第一次被执行者选择为“进行中”时记录开始时间，此规则作为 MVP 默认并允许试运行后调整；最终完成仍以 Teambition 工单终态为准。

**有效进度**:
执行者对一个核心专业子单完成本轮必填反馈。百分比跟进需选择进度档位和当前情况；里程碑跟进需选择当前里程碑、里程碑状态和当前情况；风险或阻塞时还需填写原因与所需协助。卡片不要求再次填写预计完成日期。

**周期未配置**:
核心专业子单缺少计划开始时间或截止时间，无法计算计划工作日周期的状态。系统默认采用百分比跟进并提示异常，仅通知系统管理员。

**数据异常通知**:
进度采集所需数据缺失时向系统管理员发送的管理通知。异常首次发现时立即通知；未解决异常在每个工作日 10:00 合并汇总，修复后从汇总移除并保留处理记录。

**待反馈提醒**:
进度采集发出后尚未收到有效进度时的一次再次提醒。首次采集不计入提醒次数；一个工作日后提醒一次，再过一个工作日仍无反馈则通知系统管理员，并停止本轮继续提醒。

**逾期催办**:
工作单超过截止时间且未进入终态时的提醒。逾期后首个工作日触发，随后默认每隔两个工作日触发；提交有效进度后重新计时，再过两个工作日仍未终态时恢复。与常规进度采集重合时合并为一张卡片。

**执行者**:
对单个工作单交付负责的人，与多人关系的参与者不同。

**参与者**:
与需求或工作单相关的多人关系，是默认通知对象的来源。

**人员角色**:
人员在通知规则中的业务身份，例如测试、运营和策划；一个人可以具有多个角色。只有系统管理员可以维护人员角色，转岗后的变更只影响后续通知。

**通知对象**:
接收进度或完成通知的人。通知对象从参与者及其人员角色推导，并可以按版本或需求覆盖。离职、未映射或无法投递人员不会被自动替换为其他人。
